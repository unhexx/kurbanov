# SOLVER LOOP — Практический паттерн для agentless разработки

> Основной рабочий цикл при прямой работе с моделью (без многоролевой машины).
> Применяется внутри одной сессии Grok CLI / Cursor / Blackbox.

## Пять шагов (повторять кластерами)

### 1. Inspect (осмотр)
- Прочитай **все** релевантные файлы перед любыми правками.
- Для задач по консультанту обязательно:
  - `architecture/ai_consultant_instructions.md` (матрица эскалаций, grounded-правила, persona)
  - `services/consultant_api/app/services/consultant.py` и `dialog_engine.py`
  - `services/consultant_api/app/routers/telegram.py` (особенно _rule_based_decision и ветки Perplexity)
  - Соответствующие тесты
- Используй grep + read_file. Не ленись читать большие файлы по частям.
- Если задача касается БД — посмотри модели и alembic/seed если есть.

**Выход шага**: у тебя в голове есть точная модель того, как сейчас работает кусок системы.

### 2. Define success (определение успеха)
Запиши в черновик (или прямо в сообщение):
- Какой конкретный тест должен пройти?
- Какой JSON/текст должен вернуться с эндпоинта или из Telegram-симуляции?
- Какое поле в Escalation должно заполниться (reason, collected_params, last_messages)?
- Какой ровно текст (или его отсутствие) клиент увидит в rule-based и в perplexity-режиме?

Если не можешь сформулировать measurable исход — задача ещё не готова к реализации.

### 3. Smallest vertical slice (минимальный вертикальный срез)
- Делай **только** то, что необходимо для прохождения критерия успеха.
- Один файл изменений = хорошо. Два-три — уже много.
- Добавляй тесты сразу, если это часть среза (не откладывай на "потом").
- Коммить после каждого доказанного среза.

Примеры хороших срезов для этого проекта:
- Добавил новую причину эскалации в матрицу + обработку в consultant.py + тест.
- Починил парсинг malformed ответа Perplexity → теперь всегда возвращает escalate с tech_low_confidence.
- Усилил логирование в telegram router при ошибках модели.

### 4. Proportional verification (пропорциональная проверка)
Не ограничивайся `pytest`. Обязательно:

- Ручной smoke: 
  - `python -m pytest -q --tb=line services/consultant_api/tests`
  - Запуск uvicorn в smoke-режиме + curl на /intake, /calculator, симуляция webhook.
- Для диалога — используй существующие тесты `test_telegram_consultant.py` (они хорошо покрывают rule-based и perplexity fallback).
- Прочитай итоговые логи / ответы модели.
- Проверь, что в ответах клиенту **нет** упоминаний технологий, Perplexity, "я ИИ" и т.п.

Если срез маленький — проверка тоже небольшая, но **обязательная**.

### 5. Reflect (рефлексия)
Ответь себе (в тексте, перед следующим кластером инструментов):

- Что сработало с первого раза?
- Где модель (или я) потратила лишние ходы?
- Что нужно было прочитать раньше?
- Есть ли новый постоянный урок для AGENTS.md или PROJECT_CONTEXT.md?
- Стоит ли откатить последний коммит?

Только после рефлексии — переходи к следующей задаче или расширяй текущую.

## Ограничение «3 инструмента»

Внутри одного кластера (до рефлексии) — **максимум 3 вызова инструментов** (read_file, grep, write, run и т.д. считаются).

Это заставляет быть дисциплинированным и читать больше, чем писать.

Если нужно больше 3 — сделай рефлексию раньше и начни новый кластер.

## Примеры рефлексии (хорошие)

> "Прочитал dialog_engine.py:125 и consultant.py:310. Понял, что collected_params передаются в escalation только при rule-based. Успех определил как: при tech_low_confidence от модели escalation содержит последние 6 сообщений + все собранные параметры. Срез: добавил 4 строки в perplexity_client + 1 тест. Проверка: тест прошёл, ручная симуляция через webhook дала escalation с полным контекстом. Рефлексия: в следующий раз сначала смотреть все места, где escalation создаётся, а не только perplexity_client."

## Когда прерывать цикл и звать ревью / architecture review

- После 3+ неудачных попыток пройти один и тот же тест.
- Когда изменения затрагивают матрицу эскалаций или правила grounded-ответов.
- Перед merge в main (минимум 1 ревью другим человеком или сильной моделью в режиме Reviewer).

## Инструменты, которые особенно хорошо работают в agentless

- Точное чтение + grep с контекстом.
- Запуск pytest с фильтрами на конкретный тест.
- Редактирование через search_replace (точечные правки).
- `run_terminal_command` для smoke-тестов и uvicorn в фоне (monitor).

Избегай: массовых рефакторингов без предварительного среза, "давай перепишем весь consultant.py".

---

Применяй дисциплинированно. Качество рефлексии важнее скорости.