# Agentless Loop (Solver Loop)

Лёгкий одноагентный режим разработки для сильных моделей (Grok, Claude 4, GPT-4o, MiniMax и др.) в инструментах прямого взаимодействия: этот CLI, Blackbox (прямой чат), Cursor Agent, Continue, Windsurf и т.п.

В отличие от `agentic_loop_template/` (многоролевая машина Orchestrator → Coder → Tester → Reviewer с JSON-хandoff), здесь **одна модель** последовательно проходит цикл сама, без передачи управления между ролями.

## Когда использовать agentless

- Быстрые итерации на сильной модели, где overhead от ролевой машины избыточен.
- Работа в этом Grok CLI или аналогичных "однопоточных" агентах.
- Задачи, которые хорошо ложатся на паттерн Solver Loop (см. ниже).
- Когда хочется минимального количества артефактов (без кучи handoff_*.json).

Полноценный agentic loop (с разделением ролей) оставляй для очень крупных или рискованных задач, где критична многоуровневая проверка.

## Основной цикл (Solver Loop)

Максимум **3 tool-вызова** без рефлексии внутри итерации.

1. **Inspect** — полностью прочитай релевантный код, логи, тесты, текущее состояние БД, открытые эскалации. Не начинай править, пока не понял «spine» системы.
2. **Define success** — чётко сформулируй measurable исход (тест прошёл, эндпоинт вернул ожидаемый JSON, эскалация создана с правильным reason, калькулятор дал верное число и т.д.).
3. **Smallest vertical slice** — реализуй самый маленький end-to-end кусок, который доказывает, что идея работает.
4. **Proportional verification** — проверь на поверхности пользователя: тесты + ручная симуляция (webhook, админка, калькулятор) + чтение логов. Зафиксируй доказательства.
5. **Reflect** — что сработало, что нет, нужно ли откатить через `git reset`. Только после этого расширяй scope или переходи к следующей задаче.

После рефлексии — можно новый кластер до 3 вызовов.

## Обязательные правила проекта (не нарушать)

- Все коммиты, комментарии в коде, docstrings — **только естественный русский**, голос реального mid/senior разработчика, работавшего над проектом месяцы.
- **Полный запрет** слов: AI, LLM, агент, нейросеть, MiniMax, M2.7, Grok, Claude, "as an assistant" и т.п. в коммитах и комментариях к коду.
- Любая работа с Python — **только внутри `.venv`** после вызова `setup_kurbanov.ps1`.
- Perplexity (sonar-pro) используется **исключительно** для генерации текстов ответов консультанта. Квалификация, эскалации, калькулятор — строго детерминированные.
- Бот никогда не раскрывает клиенту факт использования внешних сервисов.

## Быстрый старт (для текущей сессии / Blackbox / Cursor)

1. В начале каждой сессии (или после `git pull`) обязательно:
   ```powershell
   powershell -ExecutionPolicy Bypass -File .\agentic_loop_template\setup_kurbanov.ps1 -Smoke
   . .\.venv\Scripts\Activate.ps1
   $env:PYTHONPATH = 'services\consultant_api'
   ```

2. Прочитай в таком порядке (агрессивно суммируй длинные файлы):
   - `AGENTS.md` (этот документ — главные правила)
   - `TASK_SPECIFICATION.md`
   - `TODO.md` (текущие задачи фазы)
   - `architecture/ai_consultant_instructions.md` (истина по поведению Telegram-консультанта)
   - `PROJECT_CONTEXT.md` + `SPRINTPLAN.md` (текущий статус)

3. Выбери 1–2 задачи из TODO.md в стиле INVEST.

4. Применяй Solver Loop к каждой задаче.

5. После значимого рабочего среза — коммит на естественном русском.

6. В конце сессии обнови:
   - `PROJECT_CONTEXT.md` (что сделано, уроки)
   - `SPRINTPLAN.md` (отметь статусы задач)
   - При необходимости — `TODO.md`

## Полезные файлы для agentless-работы

- `AGENTS.md` — переносимые правила Solver Loop + постоянные ограничения kurbanov.
- `agentic_loop_template/setup_kurbanov.ps1` — bootstrap окружения (используется и в agentless).
- `services/consultant_api/tests/` — всегда прогоняй после изменений.
- `architecture/ai_consultant_instructions.md` — никогда не противоречить при работе с диалогом/эскалациями.

## Отличия от agentic_loop_template

| Аспект                  | agentic_loop_template                  | agentless_loop (Solver)             |
|-------------------------|----------------------------------------|-------------------------------------|
| Кол-во ролей            | 5 (Orchestrator, Coder, Tester...)    | 1 (модель сама)                    |
| Handoff                 | Строгий JSON по HANDOFF_SCHEMA         | Нет, только рефлексия в тексте     |
| STATE файлы             | Много (handoff_*.json)                 | PROJECT_CONTEXT + SPRINTPLAN + TODO |
| Overhead                | Высокий                                | Низкий                             |
| Лучше для               | Очень сложные задачи, новички в коде   | Сильные модели, быстрые итерации   |
| Внутренний паттерн      | Solver Loop внутри каждой роли         | Solver Loop — основной             |

## Self-improvement

Даже в agentless-режиме после 2–3 циклов делай architecture review и обновляй `PROJECT_CONTEXT.md` разделом уроков.

При систематических проблемах (модель забывает перечитать spec, делает слишком большие прыжки) — переходи на agentic или ужесточай правила в AGENTS.md.

---

**Лицензия**: MIT (как у проекта)

Используй ответственно. Все артефакты — в стиле реального разработчика.